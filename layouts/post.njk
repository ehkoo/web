{% extends "layouts/index.njk" %}

{% block head %}
{% include "layouts/el/opengraph.njk" %}
{% endblock %}

{% block content %}
<div class="flex flex-column flex-row-ns pa3">
  <div class="w-70-m w-60-l pr3-ns">
    <article class="bg-white pv3 ph4">
      <h1 class="blue f2 mb4">{{ title }}</h1>
      <p class="lh-copy mb3 tr silver">{{ date }} &mdash; {{ wordCount }} từ &mdash; Đọc trong {{ readingTime }} </p>

    {% if translation %}
      <p class="lh-copy i f4 mid-gray">Bài viết được dịch từ <a href="{{ translation }}" target="_blank" rel="nofollow">{{ translation }}</a></p>    
    {% endif %}

      <div class="content lh-copy dark-gray f4">{{ contents | safe }}</div>

      <hr class="mw3 center db bg-silver">
  
      {% if tags %}
      <div class="lh-copy mt3 flex items-center">
        <span class="silver mr3">Tags:</span>
        <ul class="list">
        {% for tag in tags %}
          <li class="dib"><a href="/chu-de/{{ tag.slug }}" class="ph2 pv1 db br2 bg-lightest-blue blue">{{ tag.name }}</a></li>
        {% endfor %}
        </ul>
      </div>
      {% endif %}
    </article>
  </div>

  <div class="w-30-m w-40-l">
    <div class="flex flex-column flex-row-ns top-1 sticky">
      <div class="w-50-l mr3-ns">
        {% include "layouts/el/widgets/tools.njk" %}
      </div>

      <div class="w-50-l">
        {% include "layouts/el/widgets/subscription.njk" %}
        {% include "layouts/el/widgets/social.njk" %}
        {% include "layouts/el/widgets/recruit.njk" %}
      </div>
    </div>

  </div>
</div>
{% endblock %}
