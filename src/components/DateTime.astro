---
import { formatDate } from '../lib'

const { className, date, dateStyle = 'long' } = Astro.props
const formatted = formatDate(date, { dateStyle })
---

<time datetime={date} class={className}>
  <strong>{formatted}</strong>
</time>
