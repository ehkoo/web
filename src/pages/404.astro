---
import { getEntry, render } from 'astro:content'

import Post from '@/layouts/Post.astro'

const entry = await getEntry('page', '404')
const { Content } = await render(entry)
---

<Post post={entry.data}>
  <Content />
</Post>
